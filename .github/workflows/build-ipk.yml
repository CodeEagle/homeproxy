name: Build ipk for HomeProxy

permissions:
  contents: write

on:
  push:
    branches:
      - 'master'
      - 'dev'
    paths:
      - 'htdocs/**'
      - 'po/**'
      - 'root/**'
      - 'Makefile'
      - '.github/**'
      - '!.github/ISSUE_TEMPLATE/**'

  pull_request:
    branches:
      - 'master'
      - 'dev'
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - 'htdocs/**'
      - 'root/**'
      - 'Makefile'
      - '.github/**'
      - '!.github/ISSUE_TEMPLATE/**'

  release:
    types:
      - published

  workflow_dispatch:

  schedule:
    - cron: '23 3 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source tree
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Generate patch against upstream
        run: |
          git remote add upstream https://github.com/immortalwrt/homeproxy.git 2>/dev/null || true
          git fetch upstream master

          BASE_COMMIT="$(git merge-base HEAD upstream/master)"
          git format-patch --stdout "${BASE_COMMIT}..HEAD" > /tmp/homeproxy-ce.patch

          if [ ! -s /tmp/homeproxy-ce.patch ]; then
            echo "Patch generation failed or produced an empty patch."
            exit 1
          fi

      - name: Checkout latest upstream source tree
        uses: actions/checkout@v6
        with:
          repository: 'immortalwrt/homeproxy'
          ref: 'master'
          path: 'build-source'

      - name: Checkout apk
        uses: actions/checkout@v6
        with:
          repository: 'alpinelinux/apk-tools'
          path: 'apk-tools'
          fetch-depth: 0
          ref: 'b5a31c0d865342ad80be10d68f1bb3d3ad9b0866'

      - name: Checkout po2lmo
        uses: actions/checkout@v6
        with:
          repository: 'openwrt/luci'
          path: 'po2lmo'
          sparse-checkout: 'modules/luci-base/src'

      - name: Apply HomeProxy CE patch to latest upstream
        run: |
          pushd build-source
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git am -3 /tmp/homeproxy-ce.patch
          echo "PATCHED_COMMIT=$(git rev-parse --short HEAD)" >> $GITHUB_ENV
          popd

      - name: Setup dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E rm -rf /var/lib/man-db/auto-update
          sudo -E apt-get update -qq
          sudo -E apt-get install -y build-essential curl fakeroot libssl-dev lua5.1 meson ninja-build

          pushd apk-tools
          meson setup build \
            -Db_lto=true \
            -Dcompressed-help=false \
            -Ddocs=disabled \
            -Dhelp=enabled \
            -Dlua_version=5.1 \
            -Ddefault_library=static \
            -Durl_backend=wget \
            -Dzstd=false \
            -Dpython=disabled \
            -Dtests=disabled \
            -Dcrypto_backend=openssl
          ninja -C build
          sudo -E meson install -C build --strip
          popd

          pushd po2lmo/modules/luci-base/src
          make po2lmo
          sudo -E install -m0755 -s po2lmo /usr/local/bin/po2lmo
          popd

          curl -fLO "https://raw.githubusercontent.com/openwrt/openwrt/master/scripts/ipkg-build"
          sudo install -m0755 ipkg-build /usr/local/bin/ipkg-build

      - name: Build ipk file
        run: |
          pushd build-source/.github
          fakeroot bash build-ipk.sh apk "${{ github.event_name }}"
          fakeroot bash build-ipk.sh ipk "${{ github.event_name }}"
          echo "APK_ASSET_NAME=$(ls *.apk)" >> $GITHUB_ENV
          echo "IPK_ASSET_NAME=$(ls *.ipk)" >> $GITHUB_ENV
          popd

      - name: Publishing APK files to GitHub Artifacts
        uses: actions/upload-artifact@v7
        if: github.event_name != 'release'
        with:
          name: ${{ env.APK_ASSET_NAME }}
          path: build-source/.github/${{ env.APK_ASSET_NAME }}
          archive: false

      - name: Publishing IPK files to GitHub Artifacts
        uses: actions/upload-artifact@v7
        if: github.event_name != 'release'
        with:
          name: ${{ env.IPK_ASSET_NAME }}
          path: build-source/.github/${{ env.IPK_ASSET_NAME }}
          archive: false

      - name: Publishing to GitHub Releases
        uses: svenstaro/upload-release-action@v2
        if: github.event_name == 'release'
        with:
          repo_token: ${{ github.token }}
          file: build-source/.github/luci-app-homeproxy-ce_*.*
          file_glob: true
          tag: ${{ github.ref }}

      - name: Publish nightly release assets
        uses: softprops/action-gh-release@v2
        if: github.event_name != 'release'
        with:
          tag_name: homeproxy-ce-nightly
          name: HomeProxy CE Nightly
          body: |
            Automated latest build of HomeProxy CE.

            Source branch: ${{ github.ref_name }}
            Patch head: ${{ github.sha }}
            Upstream patched commit: ${{ env.PATCHED_COMMIT }}
          prerelease: true
          make_latest: false
          generate_release_notes: false
          files: |
            build-source/.github/luci-app-homeproxy-ce_*.apk
            build-source/.github/luci-app-homeproxy-ce_*.ipk
          fail_on_unmatched_files: true
